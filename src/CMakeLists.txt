CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

SET(LIBRARY_OUTPUT_PATH "${PROJECT_BINARY_DIR}/lib")
AUX_SOURCE_DIRECTORY(${PROJECT_SOURCE_DIR}/src tw_src)

INCLUDE_DIRECTORIES("${PROJECT_SOURCE_DIR}/src")
ADD_LIBRARY(tw_static STATIC ${tw_src})
SET_TARGET_PROPERTIES(tw_static PROPERTIES OUTPUT_NAME "tw")